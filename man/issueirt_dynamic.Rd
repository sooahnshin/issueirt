% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/issueirt_dynamic.R
\name{issueirt_dynamic}
\alias{issueirt_dynamic}
\title{One-Shot Dynamic IssueIRT Model Fitting}
\usage{
issueirt_dynamic(
  votes_list,
  issue_codes,
  legis_list,
  colname_legis,
  colname_issue = NULL,
  colname_party = NULL,
  bills_list = NULL,
  colname_bills = NULL,
  term_name = NULL,
  left_code = 1,
  right_code = 0,
  missing_code = c(NA, 2),
  notInLegis_code = 9,
  lop = 0,
  minvotes = 0,
  anchors_name = NULL,
  liberal_code = 100,
  conservative_code = 200,
  a = 0.01,
  b = 0.001,
  rho_init = 10,
  sd_dynamic = 0.01,
  chains = 2,
  iter = 1000,
  warmup = 500,
  thin = 1,
  seed = 1,
  ideal_maxiter = 5000,
  ideal_burnin = 4800,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{votes_list}{A list of vote matrices, one for each time period/congress.
Each matrix should have rows as legislators and columns as roll calls.}

\item{issue_codes}{A list of issue codes for each period. Can be either:
\itemize{
\item A list of character/numeric vectors (one value per roll call, consistent with \code{issueirt()})
\item A list of data frames with a column containing issue labels (specify column with \code{colname_issue})
}}

\item{legis_list}{A list of legislator data frames, one for each period.}

\item{colname_legis}{Column name in legis_list containing legislator IDs.}

\item{colname_issue}{Column name containing issue labels when \code{issue_codes} is a list of data frames.
Ignored when \code{issue_codes} is a list of vectors. Default: NULL.}

\item{colname_party}{Optional column name in legis_list containing party codes.
If specified, \code{liberal_code} and \code{conservative_code} are used to find polarized
roll calls and select constraints. If NULL (default), constraints are automatically
determined based on ideal points near the 10th/90th percentiles from the initial
Bayesian IRT fit for robustness.}

\item{bills_list}{Optional list of bill data frames, one for each period.}

\item{colname_bills}{Optional column name in bills_list containing bill IDs.}

\item{term_name}{Optional vector of term/period names (e.g., c("H52", "H53", "H54")).}

\item{left_code}{Code for yea/left votes (default: 1).}

\item{right_code}{Code for nay/right votes (default: 0).}

\item{missing_code}{Code(s) for missing votes (default: c(NA, 2)).}

\item{notInLegis_code}{Code for legislators not in chamber (default: 9).}

\item{lop}{Minimum proportion of minority votes for a roll call to be included (default: 0).
Roll calls where either yea or nay votes are below this proportion are filtered out.}

\item{minvotes}{Minimum number of votes a legislator must cast to be included (default: 0).}

\item{anchors_name}{Optional vector of legislator IDs to use as anchors for post-processing.}

\item{liberal_code}{Party code for the liberal/left party (used for constraint selection).
Only used when \code{colname_party} is specified. If \code{colname_party} is NULL, this parameter is ignored
and constraints are automatically determined based on ideal points near the 10th/90th percentiles.}

\item{conservative_code}{Party code for the conservative/right party (used for constraint selection).
Only used when \code{colname_party} is specified. If \code{colname_party} is NULL, this parameter is ignored
and constraints are automatically determined based on ideal points near the 10th/90th percentiles.}

\item{a}{Hyperparameter for the prior of rho (default: 0.01).}

\item{b}{Hyperparameter for the prior of theta (default: 0.001).}

\item{rho_init}{Initial value for rho (default: 10).}

\item{sd_dynamic}{Standard deviation for dynamic prior (default: 0.01).}

\item{chains}{Number of MCMC chains (default: 2).}

\item{iter}{Total number of iterations per chain (default: 1000).}

\item{warmup}{Number of warmup iterations per chain (default: 500).}

\item{thin}{Thinning rate (default: 1).}

\item{seed}{Random seed (default: 1).}

\item{ideal_maxiter}{Maximum iterations for initial Bayesian IRT fit (default: 5000).}

\item{ideal_burnin}{Burnin for initial Bayesian IRT fit (default: 4800).}

\item{verbose}{Logical, whether to print progress messages (default: TRUE).}

\item{...}{Additional arguments passed to \code{rstan::sampling}.}
}
\value{
An \code{issueirt_dynamic_fit} object containing:
\item{stan_fit}{The Stan fit object}
\item{stan_input}{The Stan input object}
\item{posterior_summary}{The posterior summary with post-processed estimates}
\item{ideal_points}{Issue-specific ideal points}
\item{constraints}{The constraints used for post-processing}
\item{dynamic_rollcall}{The dynamic rollcall object}
\item{legis_data}{The processed legislator data}
\item{call}{The function call}
}
\description{
This is a convenience function that wraps the entire workflow for fitting a dynamic IssueIRT model.
It handles preprocessing, initial value estimation, model fitting, and post-processing
in a single function call.
}
\examples{
\dontrun{
# Using the included 1890s U.S. House data
data("us1890s_votes")
data("us1890s_issue")
data("us1890s_legis")
data("us1890s_bills")

# Option 1: Using a list of vectors (consistent with issueirt())
issue_vectors <- lapply(us1890s_issue, function(x) x$issue_label)
fit <- issueirt_dynamic(
  votes_list = us1890s_votes,
  issue_codes = issue_vectors,
  legis_list = us1890s_legis,
  colname_legis = "icpsr",
  colname_party = "party_name",
  liberal_code = "Democrat",
  conservative_code = "Republican",
  bills_list = us1890s_bills,
  colname_bills = "rollnumber",
  term_name = c("H52", "H53", "H54"),
  chains = 2,
  iter = 100,
  warmup = 50
)

# Option 2: Using a list of data frames
fit <- issueirt_dynamic(
  votes_list = us1890s_votes,
  issue_codes = us1890s_issue,
  colname_issue = "issue_label",
  legis_list = us1890s_legis,
  colname_legis = "icpsr",
  colname_party = "party_name",
  liberal_code = "Democrat",
  conservative_code = "Republican",
  bills_list = us1890s_bills,
  colname_bills = "rollnumber",
  term_name = c("H52", "H53", "H54"),
  chains = 2,
  iter = 100,
  warmup = 50
)

# View summary
summary(fit)
}

}
\seealso{
\code{\link{issueirt_dynamic_stan}} for the underlying Stan model,
\code{\link{make_dynamic_stan_input}} for manual input preparation,
\code{\link{make_dynamic_rollcall}} for creating dynamic rollcall objects
}
